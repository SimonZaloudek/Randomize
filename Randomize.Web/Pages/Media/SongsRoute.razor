@page "/songs"
@inject LocalizationService Loc

@* The Songs UI itself is a persistent component in MainLayout (so it survives
   navigation). This route just claims /songs and sets the page title. *@
<PageTitle>@Loc["songs.pageTitle"]</PageTitle>
